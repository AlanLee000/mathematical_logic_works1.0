# 严格相邻局部准引用塔

## 0. 外部基础定义

定义
$$
\delta(k)=
\begin{cases}
0,&k=0,\\
k-1,&k>0.
\end{cases}
$$

对任意集合 $X$，定义
$$
\operatorname{Seq}(X)=\bigcup_{r\in\omega}X^r.
$$

若
$$
\Gamma=\langle \gamma_0,\ldots,\gamma_{r-1}\rangle\in\operatorname{Seq}(X),
$$

则
$$
|\Gamma|=r,
\qquad
\Gamma_i=\gamma_i\quad (i<r).
$$

若 $x\in X$，定义
$$
x^\frown\Gamma=\langle x,\gamma_0,\ldots,\gamma_{r-1}\rangle.
$$

若
$$
\Delta=\langle\delta_0,\ldots,\delta_{s-1}\rangle,
$$

定义
$$
\Delta^\frown\Gamma
=
\langle
\delta_0,\ldots,\delta_{s-1},
\gamma_0,\ldots,\gamma_{r-1}
\rangle.
$$

---

## 1. 类型

定义 $\mathsf{Ty}$ 为满足下列条件的最小集合：
$$
\mathbf 0\in\mathsf{Ty},
$$
$$
\sigma,\tau\in\mathsf{Ty}
\Longrightarrow
(\sigma\Rightarrow\tau)\in\mathsf{Ty}.
$$

定义
$$
\rho:\mathsf{Ty}\to\omega
$$

为唯一满足下列递归方程的函数：
$$
\rho(\mathbf 0)=0,
$$
$$
\rho(\sigma\Rightarrow\tau)
=
\max(\rho(\sigma)+1,\rho(\tau)).
$$

---

## 2. 排序符号

定义排序符号集合
$$
\mathsf{Sort}
=
\{D_\sigma:\sigma\in\mathsf{Ty}\}
\cup
\{E_k:k\in\omega\}.
$$

定义
$$
\ell(D_\sigma)=\rho(\sigma),
$$
$$
\ell(E_k)=\delta(k).
$$

---

## 3. 函数符号与关系符号

定义 $(\mathsf{Fun},\mathsf{Rel})$ 为按逐坐标包含关系最小的有序对，使得以下条件全部成立。

### 3.1 对象层函数符号

对任意 $\sigma,\tau\in\mathsf{Ty}$，
$$
\operatorname{app}_{\sigma,\tau}\in\mathsf{Fun},
$$

并且
$$
\operatorname{arity}(\operatorname{app}_{\sigma,\tau})=2,
$$
$$
\operatorname{dom}(\operatorname{app}_{\sigma,\tau})
=
\langle D_{\sigma\Rightarrow\tau},D_\sigma\rangle,
$$
$$
\operatorname{cod}(\operatorname{app}_{\sigma,\tau})=D_\tau,
$$
$$
\ell(\operatorname{app}_{\sigma,\tau})
=
\rho(\sigma\Rightarrow\tau).
$$

---

### 3.2 变量代码构造符号

若
$$
k\in\omega,
\qquad
\alpha\in\mathsf{Sort},
\qquad
\ell(\alpha)\le k,
\qquad
i\in\omega,
$$

则
$$
\operatorname{var}_{k,\alpha,i}\in\mathsf{Fun},
$$

并且
$$
\operatorname{arity}(\operatorname{var}_{k,\alpha,i})=0,
$$
$$
\operatorname{dom}(\operatorname{var}_{k,\alpha,i})=\langle\rangle,
$$
$$
\operatorname{cod}(\operatorname{var}_{k,\alpha,i})=E_k,
$$
$$
\ell(\operatorname{var}_{k,\alpha,i})=\delta(k).
$$

---

### 3.3 函数应用代码构造符号

若
$$
k\in\omega,
\qquad
f\in\mathsf{Fun},
\qquad
\ell(f)\le k,
$$

则
$$
\operatorname{fun}_{k,f}\in\mathsf{Fun},
$$

并且
$$
\operatorname{arity}(\operatorname{fun}_{k,f})
=
\operatorname{arity}(f),
$$
$$
\operatorname{dom}(\operatorname{fun}_{k,f})
=
\langle
\underbrace{E_k,\ldots,E_k}_{\operatorname{arity}(f)\text{ 次}}
\rangle,
$$
$$
\operatorname{cod}(\operatorname{fun}_{k,f})=E_k,
$$
$$
\ell(\operatorname{fun}_{k,f})=\delta(k).
$$

---

### 3.4 关系应用代码构造符号

若
$$
k\in\omega,
\qquad
R\in\mathsf{Rel},
\qquad
\ell(R)\le k,
$$

则
$$
\operatorname{rel}_{k,R}\in\mathsf{Fun},
$$

并且
$$
\operatorname{arity}(\operatorname{rel}_{k,R})
=
\operatorname{arity}(R),
$$
$$
\operatorname{dom}(\operatorname{rel}_{k,R})
=
\langle
\underbrace{E_k,\ldots,E_k}_{\operatorname{arity}(R)\text{ 次}}
\rangle,
$$
$$
\operatorname{cod}(\operatorname{rel}_{k,R})=E_k,
$$
$$
\ell(\operatorname{rel}_{k,R})=\delta(k).
$$

---

### 3.5 等号代码构造符号

若
$$
k\in\omega,
\qquad
\alpha\in\mathsf{Sort},
\qquad
\ell(\alpha)\le k,
$$

则
$$
\operatorname{eq}_{k,\alpha}\in\mathsf{Fun},
$$

并且
$$
\operatorname{arity}(\operatorname{eq}_{k,\alpha})=2,
$$
$$
\operatorname{dom}(\operatorname{eq}_{k,\alpha})=\langle E_k,E_k\rangle,
$$
$$
\operatorname{cod}(\operatorname{eq}_{k,\alpha})=E_k,
$$
$$
\ell(\operatorname{eq}_{k,\alpha})=\delta(k).
$$

---

### 3.6 命题联结词代码构造符号

对任意 $k\in\omega$，
$$
\operatorname{neg}_k\in\mathsf{Fun},
\qquad
\operatorname{imp}_k\in\mathsf{Fun},
$$

并且
$$
\operatorname{arity}(\operatorname{neg}_k)=1,
$$
$$
\operatorname{dom}(\operatorname{neg}_k)=\langle E_k\rangle,
$$
$$
\operatorname{cod}(\operatorname{neg}_k)=E_k,
$$
$$
\ell(\operatorname{neg}_k)=\delta(k),
$$
$$
\operatorname{arity}(\operatorname{imp}_k)=2,
$$
$$
\operatorname{dom}(\operatorname{imp}_k)=\langle E_k,E_k\rangle,
$$
$$
\operatorname{cod}(\operatorname{imp}_k)=E_k,
$$
$$
\ell(\operatorname{imp}_k)=\delta(k).
$$

---

### 3.7 全称量词代码构造符号

若
$$
k\in\omega,
\qquad
\alpha\in\mathsf{Sort},
\qquad
\ell(\alpha)\le k,
$$

则
$$
\operatorname{all}_{k,\alpha}\in\mathsf{Fun},
$$

并且
$$
\operatorname{arity}(\operatorname{all}_{k,\alpha})=1,
$$
$$
\operatorname{dom}(\operatorname{all}_{k,\alpha})=\langle E_k\rangle,
$$
$$
\operatorname{cod}(\operatorname{all}_{k,\alpha})=E_k,
$$
$$
\ell(\operatorname{all}_{k,\alpha})=\delta(k).
$$

---

### 3.8 有限序列代码构造符号

对任意 $k\in\omega$，
$$
\operatorname{nil}_k\in\mathsf{Fun},
\qquad
\operatorname{cons}_k\in\mathsf{Fun},
$$

并且
$$
\operatorname{arity}(\operatorname{nil}_k)=0,
$$
$$
\operatorname{dom}(\operatorname{nil}_k)=\langle\rangle,
$$
$$
\operatorname{cod}(\operatorname{nil}_k)=E_k,
$$
$$
\ell(\operatorname{nil}_k)=\delta(k),
$$
$$
\operatorname{arity}(\operatorname{cons}_k)=2,
$$
$$
\operatorname{dom}(\operatorname{cons}_k)=\langle E_k,E_k\rangle,
$$
$$
\operatorname{cod}(\operatorname{cons}_k)=E_k,
$$
$$
\ell(\operatorname{cons}_k)=\delta(k).
$$

---

### 3.9 替换代码构造符号

若
$$
k\in\omega,
$$
$$
\Gamma,\Delta\in
\operatorname{Seq}(\{\alpha\in\mathsf{Sort}:\ell(\alpha)\le k\}),
$$

则
$$
\operatorname{sub}_{k,\Gamma,\Delta}\in\mathsf{Fun},
$$

并且
$$
\operatorname{arity}(\operatorname{sub}_{k,\Gamma,\Delta})
=
1+|\Delta|,
$$
$$
\operatorname{dom}(\operatorname{sub}_{k,\Gamma,\Delta})
=
\langle
\underbrace{E_k,\ldots,E_k}_{1+|\Delta|\text{ 次}}
\rangle,
$$
$$
\operatorname{cod}(\operatorname{sub}_{k,\Gamma,\Delta})=E_k,
$$
$$
\ell(\operatorname{sub}_{k,\Gamma,\Delta})=\delta(k).
$$

---

## 4. 句法关系符号

### 4.1 项关系符号

若
$$
k\in\omega,
$$
$$
\Gamma\in
\operatorname{Seq}(\{\alpha\in\mathsf{Sort}:\ell(\alpha)\le k\}),
$$
$$
\alpha\in\mathsf{Sort},
\qquad
\ell(\alpha)\le k,
$$

则
$$
\operatorname{Tm}_{k,\Gamma,\alpha}\in\mathsf{Rel},
$$

并且
$$
\operatorname{arity}(\operatorname{Tm}_{k,\Gamma,\alpha})=1,
$$
$$
\operatorname{dom}(\operatorname{Tm}_{k,\Gamma,\alpha})=\langle E_k\rangle,
$$
$$
\ell(\operatorname{Tm}_{k,\Gamma,\alpha})=\delta(k).
$$

---

### 4.2 公式关系符号

若
$$
k\in\omega,
$$
$$
\Gamma\in
\operatorname{Seq}(\{\alpha\in\mathsf{Sort}:\ell(\alpha)\le k\}),
$$

则
$$
\operatorname{Fm}_{k,\Gamma}\in\mathsf{Rel},
$$

并且
$$
\operatorname{arity}(\operatorname{Fm}_{k,\Gamma})=1,
$$
$$
\operatorname{dom}(\operatorname{Fm}_{k,\Gamma})=\langle E_k\rangle,
$$
$$
\ell(\operatorname{Fm}_{k,\Gamma})=\delta(k).
$$

---

### 4.3 语句关系符号

对任意 $k\in\omega$，
$$
\operatorname{Sent}_{k}\in\mathsf{Rel},
$$

并且
$$
\operatorname{arity}(\operatorname{Sent}_{k})=1,
$$
$$
\operatorname{dom}(\operatorname{Sent}_{k})=\langle E_k\rangle,
$$
$$
\ell(\operatorname{Sent}_{k})=\delta(k).
$$

---

### 4.4 证明关系符号

对任意 $k\in\omega$，
$$
\operatorname{Prf}_{k}\in\mathsf{Rel},
$$

并且
$$
\operatorname{arity}(\operatorname{Prf}_{k})=2,
$$
$$
\operatorname{dom}(\operatorname{Prf}_{k})=\langle E_k,E_k\rangle,
$$
$$
\ell(\operatorname{Prf}_{k})=\delta(k).
$$

---

## 5. 总符号集合与第 $n$ 层语言

定义
$$
\mathsf{Sym}
=
\mathsf{Sort}\cup\mathsf{Fun}\cup\mathsf{Rel}.
$$

对每个 $n\in\omega$，定义
$$
\Sigma_n
=
\{s\in\mathsf{Sym}:\ell(s)\le n\}.
$$

定义
$$
\mathsf{Sort}_n
=
\mathsf{Sort}\cap\Sigma_n,
$$
$$
\mathsf{Fun}_n
=
\mathsf{Fun}\cap\Sigma_n,
$$
$$
\mathsf{Rel}_n
=
\mathsf{Rel}\cap\Sigma_n.
$$

定义 $\mathcal L_n$ 为由
$$
(\mathsf{Sort}_n,\mathsf{Fun}_n,\mathsf{Rel}_n)
$$

生成的多排序一阶语言。

定义
$$
\mathcal L_\omega=\bigcup_{n\in\omega}\mathcal L_n.
$$

---

## 6. 外部原始语法

对每个 $k\in\omega$，定义原始项集合 $\operatorname{RawTerm}_k$ 为满足下列条件的最小集合：
$$
\alpha\in\mathsf{Sort}_k,\ i\in\omega
\Longrightarrow
\operatorname{Var}(\alpha,i)\in\operatorname{RawTerm}_k,
$$
$$
f\in\mathsf{Fun}_k,\
\operatorname{arity}(f)=r,\
t_0,\ldots,t_{r-1}\in\operatorname{RawTerm}_k
$$

蕴含
$$
\operatorname{FApp}(f,\langle t_0,\ldots,t_{r-1}\rangle)
\in
\operatorname{RawTerm}_k.
$$

定义原始公式集合 $\operatorname{RawForm}_k$ 为满足下列条件的最小集合：

若
$$
R\in\mathsf{Rel}_k,
\qquad
\operatorname{arity}(R)=r,
\qquad
t_0,\ldots,t_{r-1}\in\operatorname{RawTerm}_k,
$$

则
$$
\operatorname{RApp}(R,\langle t_0,\ldots,t_{r-1}\rangle)
\in
\operatorname{RawForm}_k.
$$

若
$$
\alpha\in\mathsf{Sort}_k,
\qquad
t,u\in\operatorname{RawTerm}_k,
$$

则
$$
\operatorname{EqForm}(\alpha,t,u)
\in
\operatorname{RawForm}_k.
$$

若
$$
\varphi\in\operatorname{RawForm}_k,
$$

则
$$
\operatorname{NegForm}(\varphi)\in\operatorname{RawForm}_k.
$$

若
$$
\varphi,\psi\in\operatorname{RawForm}_k,
$$

则
$$
\operatorname{ImpForm}(\varphi,\psi)\in\operatorname{RawForm}_k.
$$

若
$$
\alpha\in\mathsf{Sort}_k,
\qquad
\varphi\in\operatorname{RawForm}_k,
$$

则
$$
\operatorname{AllForm}(\alpha,\varphi)
\in
\operatorname{RawForm}_k.
$$

---

## 7. 外部良构项与外部良构公式

定义
$$
\operatorname{Ctx}_k=\operatorname{Seq}(\mathsf{Sort}_k).
$$

对每个
$$
\Gamma\in\operatorname{Ctx}_k,
\qquad
\alpha\in\mathsf{Sort}_k,
$$

定义
$$
\operatorname{Term}_k(\Gamma,\alpha)
\subseteq
\operatorname{RawTerm}_k
$$

为满足下列推导规则的最小关系截面。

### 7.1 变量项

若
$$
i<|\Gamma|,
\qquad
\Gamma_i=\alpha,
$$

则
$$
\operatorname{Var}(\alpha,i)
\in
\operatorname{Term}_k(\Gamma,\alpha).
$$

### 7.2 函数项

若
$$
f\in\mathsf{Fun}_k,
$$
$$
\operatorname{dom}(f)=\langle \alpha_0,\ldots,\alpha_{r-1}\rangle,
$$
$$
\operatorname{cod}(f)=\beta,
$$
$$
t_j\in\operatorname{Term}_k(\Gamma,\alpha_j)
\quad
(j<r),
$$

则
$$
\operatorname{FApp}(f,\langle t_0,\ldots,t_{r-1}\rangle)
\in
\operatorname{Term}_k(\Gamma,\beta).
$$

---

对每个
$$
\Gamma\in\operatorname{Ctx}_k,
$$

定义
$$
\operatorname{Form}_k(\Gamma)
\subseteq
\operatorname{RawForm}_k
$$

为满足下列推导规则的最小关系截面。

### 7.3 关系公式

若
$$
R\in\mathsf{Rel}_k,
$$
$$
\operatorname{dom}(R)=\langle \alpha_0,\ldots,\alpha_{r-1}\rangle,
$$
$$
t_j\in\operatorname{Term}_k(\Gamma,\alpha_j)
\quad
(j<r),
$$

则
$$
\operatorname{RApp}(R,\langle t_0,\ldots,t_{r-1}\rangle)
\in
\operatorname{Form}_k(\Gamma).
$$

### 7.4 等号公式

若
$$
\alpha\in\mathsf{Sort}_k,
\qquad
t,u\in\operatorname{Term}_k(\Gamma,\alpha),
$$

则
$$
\operatorname{EqForm}(\alpha,t,u)
\in
\operatorname{Form}_k(\Gamma).
$$

### 7.5 否定公式

若
$$
\varphi\in\operatorname{Form}_k(\Gamma),
$$

则
$$
\operatorname{NegForm}(\varphi)
\in
\operatorname{Form}_k(\Gamma).
$$

### 7.6 蕴含公式

若
$$
\varphi,\psi\in\operatorname{Form}_k(\Gamma),
$$

则
$$
\operatorname{ImpForm}(\varphi,\psi)
\in
\operatorname{Form}_k(\Gamma).
$$

### 7.7 全称公式

若
$$
\alpha\in\mathsf{Sort}_k,
\qquad
\varphi\in\operatorname{Form}_k(\alpha^\frown\Gamma),
$$

则
$$
\operatorname{AllForm}(\alpha,\varphi)
\in
\operatorname{Form}_k(\Gamma).
$$

定义外部语句集合
$$
\operatorname{Sen}_k
=
\operatorname{Form}_k(\langle\rangle).
$$

---

## 8. 第 $k$ 层句法对象集合

定义
$$
\mathbb E_k
=
(\{0\}\times\operatorname{RawTerm}_k)
\cup
(\{1\}\times\operatorname{RawForm}_k)
\cup
(\{2\}\times\operatorname{Seq}(\operatorname{Sen}_k)).
$$

元素
$$
(0,t)
$$

表示原始项对象。

元素
$$
(1,\varphi)
$$

表示原始公式对象。

元素
$$
(2,p)
$$

表示语句序列对象。

---

## 9. 外部准引用函数

对每个 $k\in\omega$，定义
$$
\ulcorner\cdot\urcorner_k:
\mathbb E_k
\to
\operatorname{Term}_{\mathcal L_{\delta(k)}}(\langle\rangle,E_k)
$$

如下。

### 9.1 项代码

若
$$
t=\operatorname{Var}(\alpha,i),
$$

则
$$
\ulcorner(0,t)\urcorner_k
=
\operatorname{var}_{k,\alpha,i}.
$$

若
$$
t=
\operatorname{FApp}(f,\langle t_0,\ldots,t_{r-1}\rangle),
$$

则
$$
\ulcorner(0,t)\urcorner_k
=
\operatorname{fun}_{k,f}
\bigl(
\ulcorner(0,t_0)\urcorner_k,
\ldots,
\ulcorner(0,t_{r-1})\urcorner_k
\bigr).
$$

### 9.2 公式代码

若
$$
\varphi=
\operatorname{RApp}(R,\langle t_0,\ldots,t_{r-1}\rangle),
$$

则
$$
\ulcorner(1,\varphi)\urcorner_k
=
\operatorname{rel}_{k,R}
\bigl(
\ulcorner(0,t_0)\urcorner_k,
\ldots,
\ulcorner(0,t_{r-1})\urcorner_k
\bigr).
$$

若
$$
\varphi=\operatorname{EqForm}(\alpha,t,u),
$$

则
$$
\ulcorner(1,\varphi)\urcorner_k
=
\operatorname{eq}_{k,\alpha}
\bigl(
\ulcorner(0,t)\urcorner_k,
\ulcorner(0,u)\urcorner_k
\bigr).
$$

若
$$
\varphi=\operatorname{NegForm}(\psi),
$$

则
$$
\ulcorner(1,\varphi)\urcorner_k
=
\operatorname{neg}_k
\bigl(
\ulcorner(1,\psi)\urcorner_k
\bigr).
$$

若
$$
\varphi=\operatorname{ImpForm}(\psi,\chi),
$$

则
$$
\ulcorner(1,\varphi)\urcorner_k
=
\operatorname{imp}_k
\bigl(
\ulcorner(1,\psi)\urcorner_k,
\ulcorner(1,\chi)\urcorner_k
\bigr).
$$

若
$$
\varphi=\operatorname{AllForm}(\alpha,\psi),
$$

则
$$
\ulcorner(1,\varphi)\urcorner_k
=
\operatorname{all}_{k,\alpha}
\bigl(
\ulcorner(1,\psi)\urcorner_k
\bigr).
$$

### 9.3 语句序列代码

定义辅助函数
$$
\operatorname{ListCode}_k:
\operatorname{Seq}(\operatorname{Sen}_k)
\to
\operatorname{Term}_{\mathcal L_{\delta(k)}}(\langle\rangle,E_k)
$$

如下：
$$
\operatorname{ListCode}_k(\langle\rangle)
=
\operatorname{nil}_k,
$$
$$
\operatorname{ListCode}_k
(
\langle\varphi_0,\ldots,\varphi_{r-1}\rangle
)
=
\operatorname{cons}_k
\Bigl(
\ulcorner(1,\varphi_0)\urcorner_k,
\operatorname{ListCode}_k
(
\langle\varphi_1,\ldots,\varphi_{r-1}\rangle
)
\Bigr)
$$

对 $r>0$ 成立。

若
$$
p\in\operatorname{Seq}(\operatorname{Sen}_k),
$$

则
$$
\ulcorner(2,p)\urcorner_k
=
\operatorname{ListCode}_k(p).
$$

---

## 10. 外部替换函数

### 10.1 项提升

对
$$
d\in\omega
$$

定义
$$
\operatorname{RaiseTerm}_{k,d}:
\operatorname{RawTerm}_k\to\operatorname{RawTerm}_k
$$

如下：
$$
\operatorname{RaiseTerm}_{k,d}
(
\operatorname{Var}(\alpha,i)
)
=
\operatorname{Var}(\alpha,i+d).
$$

若
$$
t=
\operatorname{FApp}(f,\langle t_0,\ldots,t_{r-1}\rangle),
$$

则
$$
\operatorname{RaiseTerm}_{k,d}(t)
=
\operatorname{FApp}
\Bigl(
f,
\langle
\operatorname{RaiseTerm}_{k,d}(t_0),
\ldots,
\operatorname{RaiseTerm}_{k,d}(t_{r-1})
\rangle
\Bigr).
$$

---

### 10.2 项替换

对
$$
r,c\in\omega,
\qquad
\vec u=\langle u_0,\ldots,u_{r-1}\rangle\in\operatorname{RawTerm}_k^r,
$$

定义
$$
\operatorname{ReplaceTerm}_{k,r,c}(-,\vec u):
\operatorname{RawTerm}_k\to\operatorname{RawTerm}_k
$$

如下：

若
$$
t=\operatorname{Var}(\alpha,i),
$$

则
$$
\operatorname{ReplaceTerm}_{k,r,c}(t,\vec u)
=
\begin{cases}
\operatorname{Var}(\alpha,i),&i<c,\\
\operatorname{RaiseTerm}_{k,c}(u_{i-c}),&c\le i<c+r,\\
\operatorname{Var}(\alpha,i-r),&c+r\le i.
\end{cases}
$$

若
$$
t=
\operatorname{FApp}(f,\langle t_0,\ldots,t_{s-1}\rangle),
$$

则
$$
\operatorname{ReplaceTerm}_{k,r,c}(t,\vec u)
=
\operatorname{FApp}
\Bigl(
f,
\langle
\operatorname{ReplaceTerm}_{k,r,c}(t_0,\vec u),
\ldots,
\operatorname{ReplaceTerm}_{k,r,c}(t_{s-1},\vec u)
\rangle
\Bigr).
$$

---

### 10.3 公式替换

对
$$
r,c\in\omega,
\qquad
\vec u=\langle u_0,\ldots,u_{r-1}\rangle\in\operatorname{RawTerm}_k^r,
$$

定义
$$
\operatorname{ReplaceFormula}_{k,r,c}(-,\vec u):
\operatorname{RawForm}_k\to\operatorname{RawForm}_k
$$

如下：

若
$$
\varphi=
\operatorname{RApp}(R,\langle t_0,\ldots,t_{s-1}\rangle),
$$

则
$$
\operatorname{ReplaceFormula}_{k,r,c}(\varphi,\vec u)
=
\operatorname{RApp}
\Bigl(
R,
\langle
\operatorname{ReplaceTerm}_{k,r,c}(t_0,\vec u),
\ldots,
\operatorname{ReplaceTerm}_{k,r,c}(t_{s-1},\vec u)
\rangle
\Bigr).
$$

若
$$
\varphi=\operatorname{EqForm}(\alpha,t,v),
$$

则
$$
\operatorname{ReplaceFormula}_{k,r,c}(\varphi,\vec u)
=
\operatorname{EqForm}
\Bigl(
\alpha,
\operatorname{ReplaceTerm}_{k,r,c}(t,\vec u),
\operatorname{ReplaceTerm}_{k,r,c}(v,\vec u)
\Bigr).
$$

若
$$
\varphi=\operatorname{NegForm}(\psi),
$$

则
$$
\operatorname{ReplaceFormula}_{k,r,c}(\varphi,\vec u)
=
\operatorname{NegForm}
\bigl(
\operatorname{ReplaceFormula}_{k,r,c}(\psi,\vec u)
\bigr).
$$

若
$$
\varphi=\operatorname{ImpForm}(\psi,\chi),
$$

则
$$
\operatorname{ReplaceFormula}_{k,r,c}(\varphi,\vec u)
=
\operatorname{ImpForm}
\bigl(
\operatorname{ReplaceFormula}_{k,r,c}(\psi,\vec u),
\operatorname{ReplaceFormula}_{k,r,c}(\chi,\vec u)
\bigr).
$$

若
$$
\varphi=\operatorname{AllForm}(\alpha,\psi),
$$

则
$$
\operatorname{ReplaceFormula}_{k,r,c}(\varphi,\vec u)
=
\operatorname{AllForm}
\bigl(
\alpha,
\operatorname{ReplaceFormula}_{k,r,c+1}(\psi,\vec u)
\bigr).
$$

---

### 10.4 总替换函数

若
$$
\Gamma,\Delta\in\operatorname{Ctx}_k,
\qquad
|\Delta|=r,
$$

定义
$$
\operatorname{Sub}_{k,\Gamma,\Delta}:
\mathbb E_k^{1+r}\to\mathbb E_k
$$

如下。

对
$$
a,b_0,\ldots,b_{r-1}\in\mathbb E_k,
$$

若存在
$$
u_0,\ldots,u_{r-1}\in\operatorname{RawTerm}_k
$$

使得
$$
b_j=(0,u_j)
\quad
(j<r),
$$

令
$$
\vec u=\langle u_0,\ldots,u_{r-1}\rangle.
$$

在此情形下：

若
$$
a=(0,t),
$$

则
$$
\operatorname{Sub}_{k,\Gamma,\Delta}(a,b_0,\ldots,b_{r-1})
=
\bigl(
0,
\operatorname{ReplaceTerm}_{k,r,0}(t,\vec u)
\bigr).
$$

若
$$
a=(1,\varphi),
$$

则
$$
\operatorname{Sub}_{k,\Gamma,\Delta}(a,b_0,\ldots,b_{r-1})
=
\bigl(
1,
\operatorname{ReplaceFormula}_{k,r,0}(\varphi,\vec u)
\bigr).
$$

若
$$
a=(2,p),
$$

则
$$
\operatorname{Sub}_{k,\Gamma,\Delta}(a,b_0,\ldots,b_{r-1})
=
(2,p).
$$

若不存在这样的
$$
u_0,\ldots,u_{r-1},
$$

则
$$
\operatorname{Sub}_{k,\Gamma,\Delta}(a,b_0,\ldots,b_{r-1})
=
a.
$$

---

## 11. 基础公理集合

对每个 $n\in\omega$，定义
$$
\operatorname{BaseAx}_n
\subseteq
\operatorname{Sen}_n
$$

为下列集合的并。

---

### 11.1 项正公理
$$
\Bigl\{
\operatorname{Tm}_{k,\Gamma,\alpha}
\bigl(
\ulcorner(0,t)\urcorner_k
\bigr)
:
k\le n+1,\
\Gamma\in\operatorname{Ctx}_k,\
\alpha\in\mathsf{Sort}_k,\
t\in\operatorname{Term}_k(\Gamma,\alpha)
\Bigr\}.
$$

---

### 11.2 公式正公理
$$
\Bigl\{
\operatorname{Fm}_{k,\Gamma}
\bigl(
\ulcorner(1,\varphi)\urcorner_k
\bigr)
:
k\le n+1,\
\Gamma\in\operatorname{Ctx}_k,\
\varphi\in\operatorname{Form}_k(\Gamma)
\Bigr\}.
$$

---

### 11.3 语句正公理
$$
\Bigl\{
\operatorname{Sent}_{k}
\bigl(
\ulcorner(1,\varphi)\urcorner_k
\bigr)
:
k\le n+1,\
\varphi\in\operatorname{Sen}_k
\Bigr\}.
$$

---

### 11.4 项负公理
$$
\Bigl\{
\neg
\operatorname{Tm}_{k,\Gamma,\alpha}
\bigl(
\ulcorner a\urcorner_k
\bigr)
:
k\le n+1,\
\Gamma\in\operatorname{Ctx}_k,\
\alpha\in\mathsf{Sort}_k,\
a\in\mathbb E_k,\
a\notin
\{(0,t):t\in\operatorname{Term}_k(\Gamma,\alpha)\}
\Bigr\}.
$$

---

### 11.5 公式负公理
$$
\Bigl\{
\neg
\operatorname{Fm}_{k,\Gamma}
\bigl(
\ulcorner a\urcorner_k
\bigr)
:
k\le n+1,\
\Gamma\in\operatorname{Ctx}_k,\
a\in\mathbb E_k,\
a\notin
\{(1,\varphi):\varphi\in\operatorname{Form}_k(\Gamma)\}
\Bigr\}.
$$

---

### 11.6 语句负公理
$$
\Bigl\{
\neg
\operatorname{Sent}_{k}
\bigl(
\ulcorner a\urcorner_k
\bigr)
:
k\le n+1,\
a\in\mathbb E_k,\
a\notin
\{(1,\varphi):\varphi\in\operatorname{Sen}_k\}
\Bigr\}.
$$

---

### 11.7 句法对象分离公理
$$
\Bigl\{
\ulcorner a\urcorner_k\ne\ulcorner b\urcorner_k
:
k\le n+1,\
a,b\in\mathbb E_k,\
a\ne b
\Bigr\}.
$$

---

### 11.8 替换公理
$$
\Bigl\{
\operatorname{sub}_{k,\Gamma,\Delta}
\bigl(
\ulcorner a\urcorner_k,
\ulcorner b_0\urcorner_k,
\ldots,
\ulcorner b_{|\Delta|-1}\urcorner_k
\bigr)
=
\ulcorner
\operatorname{Sub}_{k,\Gamma,\Delta}
(a,b_0,\ldots,b_{|\Delta|-1})
\urcorner_k
:
$$
$$
k\le n+1,\
\Gamma,\Delta\in\operatorname{Ctx}_k,\
a,b_0,\ldots,b_{|\Delta|-1}\in\mathbb E_k
\Bigr\}.
$$

---

### 11.9 对象层非逻辑公理
$$
\operatorname{ObjAx}_n=\varnothing.
$$

因此
$$
\operatorname{BaseAx}_n
=
\operatorname{Ax}^{\operatorname{Tm}+}_n
\cup
\operatorname{Ax}^{\operatorname{Fm}+}_n
\cup
\operatorname{Ax}^{\operatorname{Sent}+}_n
\cup
\operatorname{Ax}^{\operatorname{Tm}-}_n
\cup
\operatorname{Ax}^{\operatorname{Fm}-}_n
\cup
\operatorname{Ax}^{\operatorname{Sent}-}_n
\cup
\operatorname{Ax}^{\operatorname{Sep}}_n
\cup
\operatorname{Ax}^{\operatorname{Sub}}_n
\cup
\operatorname{ObjAx}_n.
$$

---

## 12. 外部 Hilbert 证明关系

固定一个多排序一阶 Hilbert 演算 $\mathsf H$，其每个第 $n$ 层实例给出集合
$$
\operatorname{LogAx}_{\mathsf H,n}
\subseteq
\operatorname{Sen}_n
$$

以及推理规则
$$
\operatorname{MP},
\qquad
\operatorname{Gen}.
$$

对任意
$$
A\subseteq\operatorname{Sen}_n,
\qquad
p\in\operatorname{Seq}(\operatorname{Sen}_n),
\qquad
\varphi\in\operatorname{Sen}_n,
$$

定义
$$
\operatorname{Proof}_{\mathsf H,n}(A,p,\varphi)
$$

当且仅当存在 $r\in\omega$ 与
$$
\psi_0,\ldots,\psi_{r-1}\in\operatorname{Sen}_n
$$

使得
$$
r>0,
$$
$$
p=\langle\psi_0,\ldots,\psi_{r-1}\rangle,
$$
$$
\psi_{r-1}=\varphi,
$$

并且对每个 $i<r$，至少一个下列条件成立：
$$
\psi_i\in A,
$$
$$
\psi_i\in\operatorname{LogAx}_{\mathsf H,n},
$$
$$
\exists j<i\ \exists l<i
\bigl(
\psi_l=
\operatorname{ImpForm}(\psi_j,\psi_i)
\bigr),
$$
$$
\exists j<i\ \exists\alpha\in\mathsf{Sort}_n
\bigl(
\psi_i=
\operatorname{AllForm}(\alpha,\psi_j)
\bigr).
$$

---

## 13. 证明谓词固定点

对每个 $n\in\omega$，定义
$$
\mathcal P_n
=
\mathcal P
\bigl(
\operatorname{Seq}(\operatorname{Sen}_n)
\times
\operatorname{Sen}_n
\bigr).
$$

定义
$$
\mathcal P_\omega
=
\prod_{n\in\omega}\mathcal P_n.
$$

对
$$
P=(P_n)_{n\in\omega}\in\mathcal P_\omega
$$

与
$$
n\in\omega
$$

定义
$$
\operatorname{ProofGraph}_n(P)
=
\Bigl\{
\operatorname{Prf}_{k}
\bigl(
\ulcorner(2,p)\urcorner_k,
\ulcorner(1,\varphi)\urcorner_k
\bigr)
:
k\le n+1,\
(p,\varphi)\in P_k
\Bigr\}.
$$

定义
$$
A_n(P)
=
\operatorname{BaseAx}_n
\cup
\operatorname{ProofGraph}_n(P).
$$

定义算子
$$
\Phi:\mathcal P_\omega\to\mathcal P_\omega
$$

如下：
$$
\Phi(P)_n
=
\Bigl\{
(p,\varphi)\in
\operatorname{Seq}(\operatorname{Sen}_n)\times\operatorname{Sen}_n
:
\operatorname{Proof}_{\mathsf H,n}
\bigl(
A_n(P),p,\varphi
\bigr)
\Bigr\}.
$$

在 $\mathcal P_\omega$ 上定义逐坐标包含序：
$$
P\le Q
\Longleftrightarrow
\forall n\in\omega\ (P_n\subseteq Q_n).
$$

定义
$$
\operatorname{Pref}(\Phi)
=
\{P\in\mathcal P_\omega:\Phi(P)\le P\}.
$$

定义
$$
P^\ast
=
\bigcap \operatorname{Pref}(\Phi),
$$

其中交按逐坐标理解：
$$
P^\ast_n
=
\bigcap\{P_n:P\in\operatorname{Pref}(\Phi)\}.
$$

由 $\Phi$ 的单调性与 Knaster–Tarski 定理，
$$
\Phi(P^\ast)=P^\ast.
$$

---

## 14. 理论序列

对每个 $n\in\omega$，定义
$$
T_n
=
A_n(P^\ast).
$$

于是
$$
T_n
=
\operatorname{BaseAx}_n
\cup
\operatorname{ProofGraph}_n(P^\ast).
$$

并且
$$
(p,\varphi)\in P^\ast_n
$$

当且仅当
$$
\operatorname{Proof}_{\mathsf H,n}(T_n,p,\varphi).
$$

最终系统定义为
$$
\boxed{
\mathbf H
=
\langle
\mathcal L_n,T_n
\rangle_{n\in\omega}.
}
$$

---

## 15. 相邻性等式

对任意 $k,m\in\omega$，
$$
E_k\in\mathcal L_m
\Longleftrightarrow
\ell(E_k)\le m.
$$

因此
$$
E_0\in\mathcal L_m
\quad
\text{对所有 }m\in\omega,
$$

并且对 $k>0$，
$$
E_k\in\mathcal L_m
\Longleftrightarrow
k-1\le m
\Longleftrightarrow
k\le m+1.
$$

所以对每个 $n\ge 1$，
$$
E_n\in\mathcal L_{n-1},
$$
$$
E_{n+1}\notin\mathcal L_{n-1}.
$$

同理，对每个 $n\ge 1$，
$$
\operatorname{Tm}_{n,\Gamma,\alpha}\in\mathcal L_{n-1},
$$
$$
\operatorname{Fm}_{n,\Gamma}\in\mathcal L_{n-1},
$$
$$
\operatorname{Sent}_{n}\in\mathcal L_{n-1},
$$
$$
\operatorname{Prf}_{n}\in\mathcal L_{n-1},
$$

并且
$$
\operatorname{Tm}_{n+1,\Gamma,\alpha}\notin\mathcal L_{n-1},
$$
$$
\operatorname{Fm}_{n+1,\Gamma}\notin\mathcal L_{n-1},
$$
$$
\operatorname{Sent}_{n+1}\notin\mathcal L_{n-1},
$$
$$
\operatorname{Prf}_{n+1}\notin\mathcal L_{n-1}.
$$

---

## 16. 准引用层级

对每个 $k\in\omega$，
$$
\ulcorner\cdot\urcorner_k:
\mathbb E_k
\to
\operatorname{Term}_{\mathcal L_{\delta(k)}}(\langle\rangle,E_k).
$$

若 $k>0$，则
$$
\delta(k)=k-1,
$$

所以
$$
\ulcorner\cdot\urcorner_k:
\mathbb E_k
\to
\operatorname{Term}_{\mathcal L_{k-1}}(\langle\rangle,E_k).
$$

并且
$$
E_{k+1}\notin\mathcal L_{k-1}.
$$

因此
$$
\operatorname{Term}_{\mathcal L_{k-1}}(\langle\rangle,E_{k+1})
$$

不是 $\mathcal L_{k-1}$ 的项集合。

---

## 17. 对象类型阶数

对任意 $n\in\omega$ 与 $\sigma\in\mathsf{Ty}$，
$$
D_\sigma\in\mathcal L_n
\Longleftrightarrow
\rho(\sigma)\le n.
$$

对任意
$$
\sigma,\tau\in\mathsf{Ty},
$$
$$
\operatorname{app}_{\sigma,\tau}\in\mathcal L_n
\Longleftrightarrow
\rho(\sigma\Rightarrow\tau)\le n.
$$

因此
$$
\mathcal L_n
$$

只含有阶数不超过 $n$ 的对象排序符号与对象应用符号。

---

## 18. 内部统一句法谓词的缺失

对每个 $m\in\omega$，$\mathcal L_m$ 的签名中没有排序符号 $I$ 被指定为层指标排序。

对每个 $m\in\omega$，$\mathcal L_m$ 的签名中没有二元关系符号
$$
\operatorname{Form}(i,x).
$$

对每个 $m\in\omega$，$\mathcal L_m$ 的签名中没有函数符号
$$
\operatorname{Decode}(i,x).
$$

在 $\mathcal L_m$ 中出现的公式谓词恰为分散符号
$$
\operatorname{Fm}_{k,\Gamma}
$$

中满足
$$
\delta(k)\le m
$$

的那些符号。

下标 $k$ 是外部 ZFC 中的自然数参数，不是 $\mathcal L_m$ 内部的对象变量。


---


